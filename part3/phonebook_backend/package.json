{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "A simple backend build using Node.js and express which will communicate with the frontend part of the_phonebook project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../../part2/the_phonebook && npm run build && cp -r dist ../../part3/phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin main",
    "dev": "node --watch index.js"
  },
  "author": "M-Ahmad-Usman",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.19.2",
    "morgan": "^1.10.1"
  }
}
